package router

import (
	"testing"
)

// routing test
func TestRouter(t *testing.T) {
	// r := Router()
	// w := httptest.NewRecorder()
	// req, _ := http.NewRequest("GET", "/blog/v1/", nil)
	// r.ServeHTTP(w, req)
	// assert.Equal(t, 200, w.Code)
}
